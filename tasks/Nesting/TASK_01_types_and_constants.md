# TASK 01 — Types & Constants ✅ DONE

**Status:** Complete  
**Files created:**
- `src/features/nesting/constants.ts` — Sheet dims, margins, offsets, tolerances, layer names/colors, canvas colors
- `src/features/nesting/types.ts` — NestPart, Placement, SheetLayout, NestJob, PackItem, FreeRect, PackResult, PackedBin, parseFilename, directionToRotation, computeCutDimensions, detectPackingMode, createEmptyNestJob, createNestPart, createLayoutId